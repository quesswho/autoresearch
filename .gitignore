# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results files (local experiment logs: val_bpb + benchmark scores)
results.tsv
eval_results.tsv

# Model checkpoints (large; saved per run)
checkpoints/

# Per-run logs (run.log archived as logs/<commit>.log at end of each run)
run.log
logs/

# Vendored BabyLM eval harness (external; clone of babylm-org/babylm-eval)
babylm-eval/
